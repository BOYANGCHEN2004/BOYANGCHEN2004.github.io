# Python Quick Reference

* `dictionary.get(key, default=None)`: 
    - key: The name of the key you want to look up.
    - default (optional): The value to return if the key is not found. If you don't provide this, it defaults to None. 